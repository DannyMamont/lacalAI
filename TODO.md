# TODO
- Create HTML form for user's birthdate input
- Add JavaScript logic to calculate and display user's age based on birthdate input
- Implement date format validation (YYYY) using JavaScript regex
- Display notification to user if they enter invalid date format
- Integrate Python Django backend with frontend HTML/JS code
- Create alert for invalid date format 
- Format age output as text under calculate button  
- Define a view in the Django backend to handle birthdate input and return calculated age.
- Create a template file (e.g., `age_calculator.html`) to display the form and age result.



